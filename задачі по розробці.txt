

2. написати компонент реєстрації  та авторизації.
    - має рендеритись при умові, що в локал сторейдж користувача  не має відповідного токена.
    - якщо токен є, має рендеритись кнопка лог-аута, з видаленням токена з локалл сторейдж.
3. написати сервіс авторизації.
    - має зв'язувати функціонал реєстрації  та авторизації  фронтенду, з бекендом, використовуючи відповідні ендпоінти.